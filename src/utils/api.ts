import { Spellbinder } from "spellbinder";

//Create a new instance of Spellbinder to set base url and validation the API response
export const s = new Spellbinder({
  baseUrl: "https://api.rawg.io/api",
});
